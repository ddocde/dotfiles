# Customization

Prefer profile composition and module-local changes. Keep machine-specific paths, identities, registry tokens, and company settings in local files. Standard commands are not shadowed: aliases use explicit names such as `ll`, `la`, and `gs`.
