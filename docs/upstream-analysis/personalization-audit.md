# Personalization audit findings

Machine paths, desktop state, usernames, regional settings, and personal shortcuts were mixed with reusable configuration. They are excluded here. Git identity and shell customizations use untracked local files, while regional network policy is an explicit selectable mode.
