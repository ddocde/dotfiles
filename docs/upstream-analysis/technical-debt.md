# Technical-debt findings

Priority issues were two competing installer models, a deployment bootstrap cycle, mutable Zsh startup, platform overclaims, incomplete hashes, swallowed failures, and slow monolithic verification. The current architecture gives each concern one owner and exposes explicit status and recovery information.
